# FB2 完整交接任务书

结论：当前研究位于 **S2 已完成、S3 未开始** 的边界。下一阶段不是继续增加普通点预测模型，而是先建立同管线的 FB0，再实现主张的 FB2；FB2 通过预测侧验收后，才设计和接入 B3。

本文件用于让 Claude 接手研究。它是执行入口，不替代规范、数据字典和三份研究计划；发生冲突时，以 `AGENTS.md`、`CONVENTIONS.md` 和下列权威文件为准。

## 1. 必须先读的文件

| 顺序 | 文件 | 用途 |
|---|---|---|
| 1 | `AGENTS.md` | 仓库级禁令、改动和实验红线 |
| 2 | `CONVENTIONS.md` | 术语、数据角色、证据与免责边界 |
| 3 | `data/multi_market_energy_reserve/DATA_DICTIONARY.md` | DK1 字段、单位、时间、缺失、可见性和未决规则 |
| 4 | `research-foundation-2026/RESEARCH_FRAMEWORK.md` | 研究问题、假设、实验臂和主张边界 |
| 5 | `research-foundation-2026/EXECUTION_PLAN.md` | 冻结协议、当前进度、执行顺序和验收 |
| 6 | `research-foundation-2026/PAPER_PLAN.md` | P1 证据缺口、论文边界和停止条件 |
| 7 | `configs/f01_lear_dk1.yaml`、`configs/f08_lightgbm_dk1.yaml` | 已运行预测基线的实际协议 |
| 8 | `p1_paper/results/*/summary.json` | 已完成实验的可核验结果，不以计划文字代替结果 |

## 2. 研究对象与核心问题

研究对象是一座位于 DK1、固定为 4 MW / 8 MWh 的价格接受者储能。它参与日前电能量与 aFRR 备用两个市场功能，不研究选址、扩容、策略性报价、多主体博弈、FCR 或 mFRR。

市场顺序是硬约束：aFRR 容量闸门为 D-1 07:30 CET，日前能量闸门为 D-1 12:00 CET。储能先承诺备用，后安排能量；07:30 时 D 日的日前价格、容量价格和激活均不可观测。

P1 的待验证主张是：在相同 07:30 信息集、相同物理可行域和相同结算层下，保留跨市场与跨时段相关性的联合概率预测加联合决策，是否比独立预测加顺序承诺得到更好的收益—未履约成本—备用缺口权衡。

这是假设，不是既定结论。若联合方法只改善预测误差而不改善决策结果，必须收缩论文主张。

## 3. DK1 主数据结构

主数据位于 `data/multi_market_energy_reserve/energinet_dk1/`，不是一张展平训练表，而是五张按 UTC 对齐的市场事实表。

| 表 | 主键 | 粒度与规模 | 主要字段 | 研究角色 |
|---|---|---|---|---|
| `day_ahead_prices.csv` | `TimeUTC + PriceArea` | 15 分钟；6 个价区各 32,160 时标，共 192,960 行 | `DayAheadPriceEUR` | DK1 日前价格标签及 DK2、DE、NO2、SE3、SE4 历史价格特征 |
| `afrr_capacity_market.csv` | `TimeUTC + PriceArea` | 1 小时；8,040 行 | 上下调需求、采购量和容量价 | aFRR 容量标签与市场规模 |
| `imbalance_and_activation.csv` | `TimeUTC + PriceArea` | 15 分钟；32,160 行 | 不平衡价、上下调激活量和激活加权价 | 激活标签、未履约成本和事后结算 |
| `wind_solar_forecasts.csv` | `HourUTC + PriceArea + ForecastType` | 1 小时；约 7,977 时标、3 种资源，共 23,892 行 | 五档风光预测 | 仅按发布规则使用已可见历史版本 |
| `production_consumption_settlement.csv` | `HourUTC + PriceArea` | 1 小时；7,941 行 | 发电、消费、交换和损耗 | 滞后背景特征与事后分析 |

所有连接使用 UTC。`TimeDK` 和 `HourDK` 只用于显示，因为 2025-10-26 回拨时本地钟点重复；不得把本地钟点作为主键。

日前价、不平衡价和激活保持 15 分钟网格。aFRR 容量保持小时值，并在对应四个 15 分钟槽共享同一承诺；不得复制成四个独立训练或评价样本。

`aFRRUpMW` 和 `aFRRDownMW` 虽然列名带 MW，官方含义是每 15 分钟激活电量 MWh；聚合到小时应将四个区间求和，不再乘 0.25。

## 4. 数据窗口、缺失与划分

| 项 | 冻结值 |
|---|---|
| 共同窗口 | 2025-10-01 至 2026-08-28，共 332 个交割日 |
| 整日剔除 | 2025-11-22、11-23、11-24、2026-08-25 |
| 可用交割日 | 328 |
| 滚动训练窗 | 84 天 |
| 概率校准窗 | 28 天 |
| 清除间隔 | 7 天 |
| 共同预测测试集 | 2026-01-28 至 2026-08-28，共 212 天 |
| 共同结算集 | 211 天；2026-08-26 因持续法输入落在已剔除日而排除 |
| 重校准周期 | 1 天 |

缺失值保持缺失，不补零、不插值、不外推。连续缺口按整交割日剔除；逐列缺失作为特征时保留缺失并增加缺失指示，作为标签时不进入损失和评价。

现有 F01/F08 配置写入 `calibration_window_days: 116`，对应测试前 119 个民用日扣除 3 个已剔除交割日后的可用历史；不得把它误解为修改了冻结时间边界。

## 5. 07:30 信息集

每个交割日 D 必须先生成一个决策时点快照 `X_D_0730`。任何特征都必须显式满足 `available_at <= 07:30 CET (D-1)`。

| 来源 | 07:30 时可使用范围 |
|---|---|
| 六价区日前价 | 已公布的 D-1 及更早序列；无 D 日标签 |
| aFRR 容量量价 | 已公布的 D-1 及更早序列；无 D 日标签 |
| 不平衡与激活 | 到闸门时刻 D-1 07:30（当地民用时，发布滞后为 0；S0 实测覆盖至交割日前 16.5 小时）。换算为 UTC 时冬季为 06:30、夏季为 05:30，特征构造须按 UTC 逐日换算，不得写死固定钟点 |
| 风光预测 | 只有覆盖 D-1 及更早目标时刻的已发布值 |
| 供需结算 | 约滞后 75.75 小时 |

`ForecastDayAhead` 在 D-1 18:00 才发布，晚于两个闸门，不得进入 D 日 07:30 或 12:00 的可部署方法。使用它只能形成显式标注为不可部署的 FB2+ 信息上界臂。

历史风光表的 `TimestampUTC` 是最后写入时刻，不是逐版本签发时刻。`available_at` 必须按数据字典中的发布规则推导，并记录为规则推导，而不是伪装成实测发布时间。

## 6. 观测量与模型生成量

| 类型 | 内容 |
|---|---|
| 原始市场标签 | 日前价格、aFRR 容量需求/采购/价格、不平衡价、激活量和激活价 |
| 研究模型输出 | 储能备用承诺、能量计划、实际交付、SOC、恢复动作、功率与能量缺口 |

DK1 数据不含真实储能报价、成交、SOC 和控制动作。论文只能声称“同协议仿真中方法 A 优于方法 B”，不能声称优于真实储能运营，也不能声称改善了 DK1 系统弃电或未供电量。

芬兰 aFRR、EPF-DE 和 RTS-GMLC 是不同角色的数据集，不得与 DK1 行级拼接：芬兰用于备用预测外部检验，EPF-DE 用于论文级 LEAR 复现锚定，RTS-GMLC 用于透明联合出清机制验证。

## 7. 实验臂的角色

### 7.1 预测侧

| 臂 | 定义 | 角色 |
|---|---|---|
| FB0 | 各目标独立点预测 | 必须先建立的自制对照，不是创新 |
| FB1 | 多任务点预测 | 隔离共享表示作用，目前不在冻结建设规模内 |
| FB2 | 保留跨市场、跨时段依赖的联合概率预测 | P1 的预测侧创新 |
| FB3 | 决策导向或风险校准联合预测 | 后续消融或 P3 候选 |
| FB2+ | FB2 额外获得 D 日 `ForecastDayAhead` | 违反 07:30 信息集的不可部署上界 |
| F01 | LEAR | 已发表方法的同协议点预测基线 |
| F08 | LightGBM | 已发表算法的同协议点预测基线 |

### 7.2 决策侧

| 臂 | 定义 | 角色 |
|---|---|---|
| B0 | 只做日前能量，不承诺备用 | 单市场基线 |
| B1 | 07:30 用规则承诺备用，12:00 用剩余能力做能量 | 顺序决策基线 |
| B2 | 点预测驱动联合优化 | 隔离联合优化作用的消融，已通过 F01/F08 运行 |
| B3 | 联合概率场景驱动随机或鲁棒联合优化 | P1 的决策侧创新 |
| Oracle | 同可行域完美预知 | 不可部署条件上界 |

论文核心比较是 `FB2+B3` 对 `FB0+B1`。F01/F08 的 DK1 迁移、FB0、B0/B1、B2 和 Oracle 都不是创新本身。

## 8. 已完成证据

| 阶段 | 状态 | 已核验结果 |
|---|---|---|
| R1 | 已完成 | EPF-DE 上 LEAR 1092/1456 天窗逐点复现通过，固定环境指纹 |
| S0 | 已完成 | 主键、粒度、夏令时、共同窗口、缺失、单位、聚合与两闸门可见性 |
| S1 | 已完成 | 328 天中备用占优 183 天、能量占优 145 天，证明真实机会成本存在 |
| S2 | 已完成 | B0、B1、激活感知 Oracle、激活交付、恢复成本和条件结算闭环 |
| F01/F08 | 已完成 | 两条外部点预测基线接入同一结算层 |
| R2 | 已完成 | 芬兰 aFRR 上 F08 部分复现（含验证集调参）；Up 调参后仍接近常数预测器，原论文自身也仅优于常数 4.8%，F08 实现能力未获验证 |
| S2-C | 已完成 | 将 B1 到 Oracle 的差距拆为备用联合优化与激活信息两部分 |
| S3 | 未开始 | 没有 FB0/FB2/B3 配置、实现或结果；锚定表只有 `FB2_B3` 空占位 |

211 天主实验 4 MW / 8 MWh 的已完成结果：

| 臂 | 总收益 EUR | 说明 |
|---|---:|---|
| B0 | 174,085 | 只做日前能量 |
| B1 | 433,618 | 达激活感知 Oracle 的 52.6% |
| F01 LEAR + 冻结阈值 | 461,823 | 日前 MAE 25.4578，容量 MAE 12.2719 |
| F08 LightGBM + 冻结阈值 | 445,055 | 日前 MAE 24.8421，容量 MAE 8.8125 |
| B2·F01 | 527,306 | 点预测加自由备用联合优化 |
| B2·F08 | 560,782 | 达价格与容量完美信息臂的 93.3% |
| 完美价格+自由备用 | 601,304 | 只对忽略激活信息的 B2 族构成条件参照 |
| 激活感知 Oracle | 824,744 | 当前 15 分钟时间分摊松弛下的不可部署上界 |

F08 在两个预测目标的 MAE/RMSE 上优于 F01，但在冻结阈值决策下少赚 16,768 EUR；自由备用 B2 又使 F08 高于 F01。这是 RQ4 的已完成反例，说明预测排名和决策排名可能不一致。

自由备用臂也提高了未交付率，但必须按同一价格信息成对比较：完美价格下冻结阈值 2.3% → 自由备用 7.8%；F01 预测下 8.0% → 8.8%；F08 预测下 8.8% → 12.6%；B1 为 7.4%。2.3% 只属于完美价格诊断臂，不是可部署阈值臂的基准；可部署臂的增幅为 +0.8 与 +3.8 个百分点。因此不能只报告收益，B3 必须处理可交付性风险。

## 9. 当前真正缺少的证据

P1 的甲、乙、丙三块证据已经完成；丁“联合预测＋联合优化确实兑现剩余空间”只有外部点预测和 B2 的左半边，主张方法尚未开始。

当前缺口是：

1. 同一数据管线下的 FB0 独立预测；
2. 联合预测日前价格、aFRR 容量价格与激活的 FB2；
3. 概率校准和联合场景相关性证据；
4. FB2 场景接入 B3 后的闭环决策结果；
5. 至少一条外部概率预测基线；
6. Oracle 的逐瞬时充放电/激活互斥敏感性；
7. FB2+ 对气象信息缺口的上界量化；
8. 德国同闸门市场的外部效度检验。

## 10. 开始 FB2 前必须冻结的接口

当前研究文字要求联合日前价、aFRR 容量价与激活，但现有 F01/F08 和 S2 配置主要使用 `DayAheadPriceEUR`、上调 `UpPriceEUR`、`aFRRUpMW` 与上调激活价。Claude 不得自行把范围扩成上下调双向，也不得自行把框架收缩为上调单向。

在改代码前必须向用户列出并确认：

| 决策 | 必须明确的内容 |
|---|---|
| 方向范围 | P1 是上调单向，还是上下调同时建模 |
| 输出粒度 | 日前价保持 96 个 15 分钟值还是采用 24 个小时均价；容量为 24 小时 |
| 激活表示 | 事件、方向、激活电量、激活价格分别如何建模 |
| 概率表示 | 分位数、参数分布、联合残差场景或其他可校准表示 |
| B3 接口 | 场景数量、概率权重、变量顺序、单位和时间索引 |
| 缺失标签 | 损失掩码、评价有效样本数和极端事件定义 |
| 训练窗口 | FB0 与 FB2 用冻结协议的 84 天训练 ＋ 28 天独立概率校准 ＋ 7 天清除间隔，还是沿用 F01/F08 的 116 天。FB2 需要独立校准窗，只能用 84 天；若 FB0 为对齐 F01/F08 改用 116 天，FB0 与 FB2 的差异会混入窗口长度，违反第 12 节「差异只来自联合概率结构」。无论选哪种，与 F01/F08 的窗口差异都须在结果中单独声明 |

## 11. 推荐执行顺序

1. 只读复核权威文档、F01/F08 输入矩阵、S2 求解器接口和现有结果哈希。
2. 输出文件级实施计划；预计改动超过 30 行时停止并等待确认。
3. 冻结第 10 节的目标、粒度、概率表示和 B3 数据契约。
4. 先实现 FB0，使它和 FB2 共享同一 `X_D_0730`、标签构造、滚动窗口、缺失掩码和评价代码。
5. 先实现可解释的最小 FB2：独立边际模型加训练期联合残差或经验场景；不要直接上复杂神经网络。
6. 用独立概率校准窗检查覆盖率、可靠性、锐度、跨市场相关性、跨时段相关性和极端激活覆盖。
7. 只有最小 FB2 相对 FB0 显示联合信息价值后，才评估是否需要更复杂模型。
8. FB2 预测侧验收后，另提 B3 文件级计划，将场景接入现有激活闭环和恢复层。
9. 在相同 211 个结算日上比较 `FB2+B3`、`FB0+B1`、持续法、F01、F08 和相关消融。
10. 最后执行概率外部基线、互斥敏感性、FB2+ 与德国扩展，不用这些任务阻塞最小闭环。

## 12. FB0 与 FB2 的最低验收

### FB0

- 与 FB2 使用完全相同的 07:30 特征快照和标签。
- 212 个预测交割日、211 个结算交割日与现有基线逐日一致。
- 所有输入通过 `available_at <= decision_time` 断言。
- 缺失掩码不改变原始值，标签缺失点不进入损失和指标。
- 输出预测指标、逐日预测、日索引映射、随机种子和环境指纹。

### FB2

- 联合场景保留目标之间以及一天内不同时段之间的依赖，不是把独立分位数并排命名为联合预测。
- 边际分布报告 MAE/RMSE、覆盖率、可靠性与锐度；联合分布报告相关结构和极端激活覆盖。
- 场景只用训练集拟合；超参数只用验证数据；风险阈值只用独立概率校准窗。
- 至少 3 个固定随机种子初筛，结果文件逐个记录种子和科学计算依赖精确版本。
- 与 FB0 的差异只来自联合概率结构，不得同时改变特征、窗口、损失掩码或测试日期。

### B3 后续验收

- 在同一储能容量、效率、初始/终端 SOC、市场规则和结算层下运行。
- 同时报告能量、容量、激活、恢复、未履约和总收益。
- 同时报告备用功率缺口、能量缺口、发生频率、SOC 轨迹和尾部收益。
- 以配对日期比较和置信区间判断，不用单次总收益代替稳定性证据。
- 只改善预测误差而不改善收益—风险—可交付性权衡时，判定主张未获支持。

## 13. 禁止事项

- 不修改滚动窗口、重校准周期、训练/校准/测试时间边界。
- 不修改指标、缺失值规则、储能参数、结算公式或现有科学计算依赖。
- 不让 FB2 获得 FB0/F01/F08 看不到的信息；FB2+ 必须单独标为不可部署上界。
- 不使用测试集选模型、选超参数、选场景数、选风险阈值或改变压力条件。
- 不把观测市场数据和模型生成的储能动作混写。
- 不把芬兰、德国、RTS-GMLC 与 DK1 行级拼接或直接比较跨市场 MAE。
- 不新增 fallback、mock、占位数据或吞异常的 `try/except`。
- 不因发现旁支问题顺手重构、改名、升级依赖或调整目录结构。
- 不使用 `git add -A` 或 `git add .`；只按明确路径暂存。

## 14. 文献与创新表述边界

不得声称“首次在 DK1 使用 LEAR/LightGBM”，也不得声称“首次研究丹麦储能参与日前与 aFRR”。

下列文献均**不在本地论文库**，引用前须按 `AGENTS.md` 第 1 节纳入采集器并补全 `paper_data_alignment.json`。核对依据为 2026-09-10 的 Crossref 元数据；“未核实”表示元数据不足以确认，不表示说法错误，须读全文后再定。

| 文献 | 原先的说法 | 元数据核对结果 |
|---|---|---|
| Yang, Guo, Li, Zhou (2024), *International Journal of Forecasting*, “Forecasting day-ahead electricity prices with spatial dependence”, doi:10.1016/j.ijforecast.2023.11.006 | DK1 日前价上使用 LEAR 基线 | 元数据无摘要；DK1 与 LEAR **均未核实** |
| Kılıç, Nielsen, Thibbotuwawa (2024), *Energies*, “Intraday Electricity Price Forecasting via LSTM and Trading Strategy for the Power Market: A Case Study of the West Denmark DK1 Grid Region”, doi:10.3390/en17122909 | DK1 日内价上已有 LightGBM 基线 | DK1 **已核实**（摘要覆盖 DK1 日前与日内市场）；摘要**未提及** LightGBM 或梯度提升，LightGBM 基线**未核实** |
| Cremoncini 等 (2024), *Journal of Energy Storage*, “Optimal participation of a wind and hybrid battery storage system in the day-ahead and automatic frequency restoration reserve markets”, doi:10.1016/j.est.2024.112309 | 丹麦风电与混合储能参与日前和 aFRR | 题目与方向**已核实**，引用 Energinet；DK1 价区**未在元数据中出现**。与 P1 决策侧最接近，须精读 |
| 2026 年 DTU 学位论文 | 已建模 DK1 07:30 与 12:00 两闸门及激活收益，使用 EV 聚合商、代理报价曲线及完美价格预知 | **无出处，无法核实**；补全题目、作者与链接前不得引用 |

注意与本地论文库的 M02（*Applied Energy* 2025，doi:10.1016/j.apenergy.2025.126773，风电加混合储能多市场收益优化，仅有来源卡）区分，二者不是同一篇。

可辩护的创新候选是这些条件的交集：后 PICASSO DK1 制度、07:30 决策时点可用信息、日前价—容量价—激活联合概率预测、真实闸门顺序、统一价格接受者储能可行域，以及样本外激活结算与可交付性评价。正式投稿前仍需用 Scopus、Web of Science、IEEE Xplore 和学位论文库完成可复查检索。

## 15. 停止条件

- 若 FB2 不能优于简单联合残差场景，不继续堆叠复杂模型。
- 若 FB2 只改善边际误差而不改善联合校准或 B3 决策结果，收缩“联合预测价值”主张。
- 若收益提高伴随同等或更大的备用缺口，只报告权衡，不宣称方法更优。
- 若结果只在单一容量、单一季节或单一随机种子成立，不支持普遍优势。
- 若关键 aFRR 交付持续时间或上下调资格规则仍无一手依据，相关结论保持条件性。
- 若需要修改冻结协议，停止执行，说明会使哪些既有结果失效，并等待用户确认。

## 16. Claude 的第一轮输出要求

收到本文件后先进行只读审计，不立即改代码。第一轮只回答：当前 Git/结果身份、FB2 与现有代码的接口位置、第 10 节七项未决选择、拟修改或新建的精确文件、每个文件的职责、验证命令和停止条件。

用户明确说“开始执行”后，才按已确认范围实施。完成一次获批任务后只报告：改了哪些文件、各文件改了什么、如何验证。
