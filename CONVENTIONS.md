# 术语与证据边界

本文件是全仓库术语、数据角色和免责规则的唯一定义处。其他文档引用此文件，不重述。

## 1. 研究表述边界

| 情形 | 正确表述 | 禁止表述 |
|---|---|---|
| 只预测风光/负荷，随后优化两个市场 | 预测驱动的多市场协同调度 | 多市场联合预测 |
| 同时预测两个及以上市场的结果或状态 | 多市场联合预测 | — |
| 用真实市场观测驱动的储能仿真 | 价格接受者仿真 | 现场运营复现、现场设备实验 |
| 优化器在给定规则与物理约束下产生的价格、成交量、弃电、缺口 | 模型出清结果 | 实网历史观测、真实市场数据 |
| 论文作者报告的指标 | `paper-reported` | 本仓库已复现 |
| 采集成功、校验和通过、PDF 可读 | 资源可核验 | 结果已复现 |
| 在 DS4 上运行从旧库迁入的骨干模型 | 同协议迁移评测 | 复现其原论文结果 |
| 备用容量闸门早于日前能量闸门的市场（欧洲大陆与北欧） | 两闸门顺序市场 | 该结构普遍存在；美国日前拍卖与澳洲实时出清均为能量—备用同时联合出清 |
| 价格接受者储能在 DK1 上的仿真结果 | 同可行域、同信息集下的方法比较 | 改善了新能源消纳、弃电或供电充裕度 |
| 本项目编写的 FB0、FB1、B0、B1、Oracle | 自制对照臂 | 现有技术水平、文献最优、强基线 |
| 从论文库实现并接入同协议评测的方法 | 外部基线 | 复现其原论文结果 |

"论文—数据一一对应"表示每篇文献都有明确的 `original_data_status` 和至少一条本地数据关系，关系类型分为：论文精确公开数据、同市场部分字段、当前项目迁移基准、机制仿真基准。它不表示为每篇论文复制一份 CSV，也不把未公开的原论文数据伪装成本地已有数据。

## 2. 编号体系

全库只允许以下七套编号，每套只有一个含义。新增编号前先在此登记。

| 前缀 | 含义 | 定义处 |
|---|---|---|
| `S0`–`S5` | 执行阶段，唯一权威的工作分解 | [执行计划](research-foundation-2026/Q3_NEXT_STEP_EXECUTION_PLAN_2026-09-06.md) |
| `RQ1`–`RQ5` | 核心研究问题，每条配一句可证伪主张 | [研究目标](research-foundation-2026/MULTI_MARKET_FORECASTING_RESEARCH_TARGET_2026-09-07.md) 第 5 节 |
| `FB0`–`FB3` | 预测侧基线，是被比较的对象而非任务 | [执行计划](research-foundation-2026/Q3_NEXT_STEP_EXECUTION_PLAN_2026-09-06.md) |
| `B0`–`B3`、`Oracle` | 决策侧策略臂，是被比较的对象而非任务 | [执行计划](research-foundation-2026/Q3_NEXT_STEP_EXECUTION_PLAN_2026-09-06.md) |
| `R1` | 复现任务 | `p1_paper/results/r1_lear_de/` |
| `DS1`–`DS4` | 数据集角色 | 本文件第 3 节 |
| `F` / `M` / `L` / `D` / `X` + 两位数 | 论文库编号，按分问题分组 | [论文库](paper/multi_market_energy_reserve/README.md) |

`W1`–`W4` 只是 `p1_paper/gate_research_map.html` 的展示层标签，用于按问题组织该页内容，不构成工作分解；该页每行给出对应的 S 阶段。文档、提交信息和结果目录一律使用 S 编号。

`C1`–`C4` 已废弃，其内容并入 `RQ1`、`RQ2`、`RQ4`、`RQ5` 的可证伪主张。

预测基线曾编为 `F0`–`F3`，与论文库预测组的 `F01`–`F15` 只差一位数字，现改为 `FB0`–`FB3`。`P1`–`P4` 是论文序号（`p1_paper/` 即第一篇），不得用于基线。

数据集编号使用 `DS` 前缀，避免与论文库中可交付性组的 `D01`–`D03` 冲突。

## 3. 数据角色

四套数据角色不同，**禁止拼接成同一个真实物理系统，禁止行级拼接**。

| 代号 | 数据 | 角色 | 不能声称什么 |
|---|---|---|---|
| DS1 | Energinet DK1 | 真实多市场主数据 | 不含真实储能报价、成交、SOC 和控制动作 |
| DS2 | 芬兰 aFRR（Zenodo 17494556） | 备用预测外部基准 | 不是完整的电能量—备用联合交易记录 |
| DS3 | RTS-GMLC | 联合出清机制基准 | 不是实网历史市场记录 |
| DS4 | EPF-DE（Zenodo 4624805） | 论文精确预测基准 | 不含备用市场 |
| — | 中国省内数据 | 后续迁移 | 当前不足以支持中国省级多市场实证结论 |

原始量与模型生成量必须分栏记录。DS1 的日前价格、aFRR 容量价格、不平衡与激活量是原始市场标签；储能报价、成交、SOC 和控制动作由研究模型产生。DS3 若由优化出清生成价格、成交和备用结果，必须同时公开目标函数、约束和价格提取方式。

## 4. 数据处理规则

- 以 UTC 为主键。夏令时切换和 15 分钟/小时级连接不得按本地钟点去重。
- 缺失值保持缺失。禁止静默补零、插值、外推或按默认值填充，直至完成字段语义核验。
- 时间序列禁止随机切分。训练、验证、测试按时间划分。
- 每个特征必须记录在决策时刻是否已经可见，并有 `available_at <= decision_time` 检查。`ForecastCurrent` 及事后结算数据不得作为日前任务的输入。
- 15 分钟到小时的聚合规则须预先固定：功率、能量、价格、激活量各用各的统计量，不混用。
- 发布滞后（如供需结算数据晚约 99 小时）与真实内部缺口分开记录，不为对齐而外推测试尾段。
- 重新采集官方数据得到的是当时的回溯版本。若官方修订历史值，保留新旧清单与校验和，不覆盖后冒充同一快照。

## 5. 文献质量分层

| 目录 | 含义 |
|---|---|
| `01_top_journals` / `01_top_journal_or_conference` | 已核验正式发表。锚定 IEEE TPWRS/TSG、Applied Energy、Management Science、RSER，以及 NeurIPS/ICML 正式论文集 |
| `02_peer_reviewed_specialized` | 已正式同行评审，但不作为本课题 Top 锚点 |
| `03_preprints` | 只能核实到 arXiv、Optimization Online 或在审稿版本 |

质量等级依据**核验后的最终发表载体**，不依据 PDF 文件来自哪里。本地文件可能是作者接受稿或 arXiv 版本。

## 6. 实验公平性

所有对比方法必须共享：同一储能功率、容量、效率、初始与终端 SOC；同一交易时序与数据可见性；同一市场规则与备用交付规则；同一压力场景划分。

禁止让新方法获得更多未来信息或更宽松的可行域。普通能量调度与备用承诺不得重复使用同一份功率、能量或网络送出能力。

完美预知 Oracle 只作为不可部署的上界参考，不作为方法结果。
