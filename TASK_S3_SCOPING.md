# 任务 S3 前置研究：同问题对比方法、数据特点、问题清单、可借用结构

目标：在动手做联合概率预测之前，先弄清四件事——要和哪些解决同样问题的论文比、DK1 数据有什么特点、由此遇到哪些问题、顶会里有哪些结构可以拿来解决这些问题。**本任务只调研和分析，不实现新方法。**

基线：`main` = `6f99686`。环境固定为 `/public/ZLCODE/.venvs/r1_lear_de/bin/python`。本文件在任务完成并合并后删除。

**分支与 PR**：从 `main` 开分支 `codex/s3-scoping`，全部改动提交在分支上，完成后开 PR，**等用户确认后再合并**，不要直接推 `main`。

**与 `FB2_TASK.md` 的关系**：两者冲突时以本文件为准，涉及的是 `FB2_TASK.md` 第 7.1 节（对比方法）、第 11 节（执行顺序）和第 14 节（文献）。`FB2_TASK.md` 其余各节（数据结构、信息集、禁止事项、停止条件等）继续有效。

---

## 0. 已确定的研究定位（不要重新论证）

### 0.1 要解决的问题

每天前一天早上 07:30 要决定第二天承诺多少 aFRR 上调备用。这时第二天的**日前电价、aFRR 上调容量价、aFRR 上调激活量**都还不知道。新方法要给出这三样东西的**联合概率分布**，交给后面的随机或鲁棒联合优化，去决定备用承诺多少、电量留多少。

论文最终比较的是「联合概率预测＋联合优化」对「独立点预测＋顺序决策」，看收益、备用未交付率和尾部风险三者的权衡。只改善预测误差、不改善决策结果，主张不成立。

### 0.2 为什么必须是「联合」和「概率」（已有实测结果）

| 实测结果 | 含义 |
|---|---|
| 价格点预测能争取的空间只占缺口 9.3%，LEAR 已拿到其中 77.2% | 继续提高点预测精度收益很小 |
| 事先知道激活量值 223,440 EUR，占缺口 57.1%，目前没有任何方法预测激活 | 必须把激活量纳入联合分布 |
| 点预测接联合优化后，LightGBM 这一组备用未交付率从 8.8% 升到 12.6% | 分布尾部要针对交付风险校准 |
| LightGBM 预测精度全面高于 LEAR，阈值规则下收益反而更低；换成联合优化后排名又翻过来 | 评价要看决策结果，不能只看误差 |

### 0.3 方法方向（第 4 步完成后由用户最终确认）

- **按闸门顺序分解联合分布**：先建容量价和激活量的联合分布，再建「给定容量结果后的日前价」分布；12:00 容量价公布后可更新第二步。
- **针对交付风险校准**：直接校准「按这个分布做决策后交付不了」的概率，争取给出有限样本保证（例如共形风险控制）。

### 0.4 三类方法的角色——必须分清

| 角色 | 来源 | 在论文里的位置 |
|---|---|---|
| **对比方法** | **解决同样问题的论文** | 主结果表，和本文方法正面比较 |
| **借用的结构** | 顶会（NeurIPS、ICML、ICLR、KDD 等）的方法 | 只作为新方法里的零件；在消融实验里逐个拿掉以证明有用。**不作为对比对象** |
| **自建对照** | 独立点预测、独立概率预测、顺序决策等 | 主结果表，说明增益来自哪里 |

### 0.5 数据

主实验只用 DK1。芬兰 aFRR 数据上的外部检验是否纳入本篇论文，由用户另行决定，**不在本任务范围内**。

---

## 1. 名称与仓库代号对照

文档和提交信息里首次出现时写「名称（代号）」，之后可只写名称。

| 名称 | 代号 |
|---|---|
| 独立点预测 | FB0 |
| 独立概率预测（消融，暂无代号） | — |
| 联合概率预测（本文预测侧方法） | FB2 |
| 联合概率预测＋第二天风光预测（不可部署上界） | FB2+ |
| 只做日前能量 | B0 |
| 顺序决策（先按阈值承诺备用，再排电量） | B1 |
| 点预测＋联合优化 | B2 |
| 随机或鲁棒联合优化（本文决策侧方法） | B3 |
| 完美预知上界（含激活信息） | Oracle |
| 完美价格参照（不含激活信息） | oracle_price_reserve |
| LEAR / LightGBM | F01 / F08 |

---

## 2. 第 1 步：梳理「同问题」论文

### 2.1 什么算「同问题」

本文的问题是：**储能在日前电能量和备用两个顺序市场里，先预测、再决策**。按接近程度分三级：

| 级别 | 条件 |
|---|---|
| 一级：同问题、同市场 | 储能（含与风光配套的储能）参与日前加 aFRR，研究对象为 DK1 或丹麦 |
| 二级：同问题、其他市场 | 储能参与日前（或日内）加备用市场，含预测与决策，市场不限 |
| 三级：同一个子问题 | 为储能或备用决策做价格、激活预测，并用决策结果评价预测 |

### 2.2 论文库里已有的候选（先梳理这些）

| 名称（库编号） | 载体 | 做了什么 | 原始数据状态 |
|---|---|---|---|
| 丹麦风电混合储能日前加 aFRR（M08） | J. Energy Storage 2024 | 丹麦日前加 aFRR，鲁棒 MILP，只有来源卡 | 案例输入未公开 |
| 西班牙预测—优化一体化框架（X02） | Energy Engineering 2026 | 多市场产品分别预测，接统一储能优化 | 西班牙数据未采集 |
| 德国顺序市场优化（M07） | arXiv 2026 | 按 FCR→aFRR→日前→日内门限时序滚动预测再优化，仅单日验证 | 德国市场与预测输入未公开 |
| 日前加 aFRR 储能经济评价（M06） | Applied Energy 2021 | 日前加 aFRR 场景经济评价 | 场景输入未完全公开 |
| 日内加 FCR 联合报价（X04） | arXiv/SSRN 2025 | 动态日内—FCR 策略 | 德国订单簿数据未公开 |
| 储能参与市场的鲁棒时间离散（X05） | Optimization Online/arXiv 2026 | FCR 加套利，鲁棒优化 | 场景未打包公开 |
| 丹麦储能现场三年案例（X01） | arXiv 2026 | 电池加电解槽多市场参与，基于现场性能 | 市场数据未公开 |
| 概率预测与电池经济价值（F07） | arXiv 2026 | 多变量概率模型统计上更优，但风险厌恶策略的最高利润不总来自统计最优模型 | 交易数据未作为本地快照发布 |
| 决策导向的储能套利预测（L03） | IEEE TSG 2022 | 用决策损失训练价格预测，单一能量市场 | PJM 六年数据未本地化 |
| 共形预测用于日前与平衡市场（X03） | Energy and AI 2025 | 价格预测加共形区间，并做交易评价 | 爱尔兰数据未采集 |

对每篇逐一核对：属于哪一级；预测部分是点预测、独立概率还是联合概率；决策部分是什么；**在 DK1 上按同一协议重新实现是否可行**（方法描述是否足够、能否放进 07:30 信息集、决策能否接入现有结算层）。

### 2.3 库外检索

- **数据库**：能访问的都用（Scopus、Web of Science、IEEE Xplore、Google Scholar、DTU Orbit、arXiv 等）。**访问不了的，在记录里写「未检索」，不得声称已检索。**
- **关键词组合**：市场词（DK1、Denmark、Danish、Energinet、Nord Pool、Nordic、aFRR、FRR、regulating power、balancing、imbalance price、reserve capacity）× 主体词（battery、energy storage、BESS、hybrid power plant）× 方法词（probabilistic、quantile、scenario、joint、multivariate、copula、stochastic、robust、bidding、day-ahead）。
- **时间**：不限起点，但逐篇标注数据年份。**DK1 的 aFRR 市场在 2024 年 10 月改为本地每日拍卖、按小时出清的容量市场**（见 `DATA_DICTIONARY.md` 第 24 行），此前的论文方法可借用、结果不能直接比。
- **核验**：每篇核对 DOI、正式发表载体与年份；预印本按预印本记录。

### 2.4 入选「对比方法」的条件

1. 属于 2.1 的一级或二级；三级论文只在其方法能直接接入本文决策流程时入选；
2. 方法描述足够在 DK1 上重新实现，或有公开代码；
3. 能放进 07:30 信息集，不依赖那时还看不到的数据；
4. 决策部分能接入现有结算层，或其预测部分能接入现有联合优化；
5. 发表载体已核验。

### 2.5 产出

- **新检到、要引用的论文**按已有收录流程入库：采集器（开放获取的放 PDF，否则来源卡）、catalog、论文—数据对应表、分组索引、结果表，研究截止日同步更新，最后资源审计必须通过。
- **`research-foundation-2026/PAPER_PLAN.md` 新增一节「同问题对比方法」**，两张表：
  - 候选表：名称、库编号、级别、预测方式、决策方式、数据年份（2024-10 前或后）、能否在 DK1 上重新实现及理由、是否入选；
  - 检索记录表：数据库、检索式、检索日期、命中数、初筛保留数、最终保留数；未检索的数据库也列出。
- **如果没有任何一级或二级论文做了联合概率预测**，在该节写明，并保留完整检索记录作为「该问题尚无联合概率预测工作」的依据。

---

## 3. 第 2 步：分析 DK1 数据特点（只用回看期）

### 3.1 范围——这条是红线

**只分析回看期**：2025-10-01 至 2026-01-27，116 个可用交割日（剔除 2025-11-22、11-23、11-24）。测试期（2026-01-28 起）的数据**不得以任何形式进入这一步**。分析结果将用来设计方法，用测试期数据就等于拿测试集设计方法。代码里要有断言：读入的交割日全部早于 2026-01-28。

### 3.2 分析对象

| 目标 | 表与字段 | 粒度 |
|---|---|---|
| DK1 日前电价 | `day_ahead_prices.csv` 的 `DayAheadPriceEUR`（PriceArea=DK1） | 15 分钟 |
| aFRR 上调容量价 | `afrr_capacity_market.csv` 的 `UpPriceEUR` | 1 小时 |
| aFRR 上调激活量 | `imbalance_and_activation.csv` 的 `aFRRUpMW`（每 15 分钟激活电量，单位 MWh） | 15 分钟 |
| aFRR 上调激活价 | `imbalance_and_activation.csv` 的 `aFRRVWAUpEUR` | 15 分钟 |

输入特征只做可见性核对，不做建模：其他 5 个价区日前价、风光预测（只用 07:30 前已发布、覆盖前一天及以前的版本）。

### 3.3 要回答的问题

| 编号 | 问题 | 输出 |
|---|---|---|
| D1 | 每个目标的分布：均值、分位数、偏度、尖峰占比、负值占比、零值占比（激活量） | 分布统计表 |
| D2 | 三个目标之间的依赖：同一时段和跨时段的秩相关（Spearman、Kendall），以及上尾、下尾依赖（高分位同时出现的频率） | 依赖统计表 |
| D3 | 时间结构：日内各时段的自相关、跨天自相关（滞后 1 天、7 天）、工作日与周末差异 | 时间结构表 |
| D4 | 07:30 的信息量：只用 07:30 可见的数据时，前一天同时段值、前 7 天均值等简单量能解释第二天多少变化 | 相关或解释度表 |
| D5 | 数据量：84 天训练窗口内每个目标的有效样本数、极端值出现次数；以及联合分布要估计的维度 | 样本量表 |
| D6 | 稳定性：回看期前后半段各项统计是否明显漂移 | 前后半段对比表 |

### 3.4 数据规则

- UTC 为主键，按民用交割日（Europe/Copenhagen）分组；夏令时日按实际长度处理并记录。
- **缺失值保持缺失**，不补零、不插值；每项统计写明有效样本数。
- `aFRRUpMW` 按官方语义是 MWh，15 分钟聚合到小时时四个值求和，不再乘 0.25。
- **2026-03-04 的定义变更只涉及 mFRR 的 `SatisfiedDemand` 与 `DominatingDirection`**，不影响上述四个目标；回看期也早于该日期。
- 所有输入需通过 `available_at <= decision_time` 断言。

### 3.5 文件

| 文件 | 内容 |
|---|---|
| `experiments/s3_dk1_target_profile.py` | 运行入口 |
| `configs/s3_dk1_target_profile.yaml` | 回看期边界、剔除日、分位数、滞后阶数等全部参数 |
| `p1_paper/results/s3_dk1_target_profile/summary.json` | 环境指纹、随机种子、D1–D6 汇总 |
| `p1_paper/results/s3_dk1_target_profile/*.csv` | D1–D6 各表 |
| `data/multi_market_energy_reserve/DATA_DICTIONARY.md` | 新增一节「预测目标的数据特点（回看期）」，**不超过 40 行**，数字从结果文件读出后填写，不手算 |

不建 `src/` 包：纯描述性统计，拆包属于 `AGENTS.md` 第 3 条禁止的预留抽象层。

---

## 4. 第 3 步：归纳「问题 → 要求」

- 在 `research-foundation-2026/RESEARCH_FRAMEWORK.md` 新增一节「联合概率预测必须满足的要求」，一张表：**编号、问题、来源、对方法的要求、怎么检验**。
- 问题来源只有两类：第 2 步的数据特点（写明对应 D1–D6 哪一项及数值），以及第 1 步中同问题论文没有解决的地方（写明是哪篇、哪里没解决）。**不得写没有依据的问题。**
- 每条要求必须可检验，例如「极端激活时段的覆盖率不低于名义水平」，不能写「更好地刻画依赖」这类无法判断的话。
- 该节不超过 50 行，全文件不得超过 400 行。

---

## 5. 第 4 步：可借用结构候选（只列，不实现）

- 按第 3 步的每条要求，到 NeurIPS、ICML、ICLR、KDD，以及 IEEE TPWRS/TSG、Applied Energy、International Journal of Forecasting 等检索可借用的结构。
- 可参考的方向（**以下仅为检索起点，逐篇核验载体后才能写入**）：Copula 类联合分布（如低秩高斯 Copula 过程、TACTiS / TACTiS-2）；时间序列基础模型（如 Moirai、Chronos，用于缓解数据短）；共形预测类（如共形风险控制、多步时序的 Copula 共形预测、共形 PID 控制）；扩散生成模型（如 TimeGrad、CSDI，需评估小样本风险）。
- 在 `RESEARCH_FRAMEWORK.md` 第 4 节新增的同一节里追加候选表：**结构、代表论文与已核验载体、满足哪条要求、在 84 天训练窗口下的风险、是否有官方代码**。
- 要引用的论文按 2.5 的收录流程入库。
- **本步只列候选，不选定、不实现。完成后停下，交用户确认方法设计。**

---

## 6. 对比方案草案（第 4 步后由用户确认，本任务不实现）

**预测侧**

| 类别 | 方法 |
|---|---|
| 已有点预测基线 | 持续法、LEAR、LightGBM（结果已有，保留） |
| 自建对照 | 独立点预测；独立概率预测（消融：边际分布与本文方法相同，但去掉目标间、时段间相关性） |
| 简单联合基线 | 边际分布＋训练期历史误差组合成联合场景 |
| **同问题论文的方法** | 第 1 步入选的论文，按 DK1 协议重新实现 |
| 本文方法 | 按闸门顺序分解、针对交付风险校准的联合概率预测 |
| 不可部署上界 | 本文方法＋第二天风光预测 |

**决策侧**（所有预测都接同一结算层）

| 组合 | 作用 |
|---|---|
| 独立点预测＋顺序决策 | 论文核心对比的对照组 |
| 各点预测＋联合优化 | 只换决策方式的效果 |
| 同问题论文的预测与决策方法 | 与已有工作正面比较 |
| **本文预测＋随机或鲁棒联合优化** | 本文方法 |
| 独立概率预测＋随机或鲁棒联合优化 | 相关性在决策上的价值 |
| 完美价格参照、完美预知上界 | 参照，写明各自的信息集 |

---

## 7. 不要做

1. 不实现联合概率预测、独立点预测或任何新模型。
2. **第 2 步不得读取测试期（2026-01-28 起）的任何数据。**
3. 不把顶会方法列为对比方法；它们只能出现在第 4 步的「可借用结构」表里。
4. 不改冻结协议：时间划分、滚动窗口、指标定义、缺失值规则、储能参数、结算公式。
5. 不改 `src/epf_harness/` 与已有实验的任何文件，不重跑已有实验。
6. 不补缺失值；不把芬兰、德国、RTS-GMLC 数据与 DK1 行级拼接。
7. 未核验载体的论文不写入研究文档；访问不了的数据库不得写成已检索。
8. `EXECUTION_PLAN.md` 已 397 行，本任务不往里追加内容。
9. 不新建 README、SUMMARY、CHANGELOG、`*_GUIDE.md`、`*_NOTES.md`、`*_REPORT.md`。
10. 不用 `git add -A` 或 `git add .`；不直接推 `main`。

---

## 8. 验收

**第 1 步**
- `PAPER_PLAN.md` 新增的两张表完整；每篇候选都有级别、预测方式、决策方式、数据年份和可行性判断；
- 新入库论文通过资源审计：
```bash
cd /public/ZLCODE/electricity-price-forecasting-research && /public/ZLCODE/.venvs/r1_lear_de/bin/python scripts/audit_multi_market_resources.py
```
退出码为 0。在 git 工作树（worktree）里运行时，若只缺被 `.gitignore` 排除的 `day_ahead_prices_raw.json`，从主检出硬链接补齐后重跑，不得放宽断言。

**第 2 步**
```bash
cd /public/ZLCODE/electricity-price-forecasting-research && OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 /public/ZLCODE/.venvs/r1_lear_de/bin/python experiments/s3_dk1_target_profile.py --config configs/s3_dk1_target_profile.yaml
```
- 断言「全部交割日早于 2026-01-28」通过，116 个交割日；
- `summary.json` 含环境指纹与随机种子，D1–D6 齐全，每项统计有有效样本数；
- 重跑两次结果一致；
- 已有实验结果目录零差异（`git diff --stat p1_paper/results/` 只出现 `s3_dk1_target_profile/`）。

**第 3、4 步**
- `RESEARCH_FRAMEWORK.md` 新增一节，含「问题 → 要求」表和可借用结构候选表，每条问题都有数据或文献依据，每条要求可检验；
- 全文件不超过 400 行。

---

## 9. 提交与停止点

- 第 1 步、第 2 步、第 3＋4 步各一次提交，commit message 写明新增内容与关键发现。
- 完成后开 PR（`codex/s3-scoping` → `main`），在 PR 描述里用三五句话总结：找到几篇同问题论文、有没有做联合概率预测的、数据最突出的三个特点、最关键的三条要求。
- **开 PR 后停下，等用户确认方法设计与对比方案，再进入实现。**
