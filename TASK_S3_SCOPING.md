# 任务 S3 前置研究（子代理分工版）：同问题对比方法、数据特点、问题清单、可借用结构

目标：在动手做联合概率预测之前，先弄清四件事——要和哪些解决同样问题的论文比、DK1 数据有什么特点、由此遇到哪些问题、顶会里有哪些结构可以拿来解决这些问题。**本任务只调研和分析，不实现新方法。**

基线：`main` 最新提交。环境固定为 `/public/ZLCODE/.venvs/r1_lear_de/bin/python`。本文件在任务完成并合并后删除。

**分支与 PR**：从 `main` 开分支 `codex/s3-scoping`，全部改动提交在分支上，完成后开 PR，**等用户确认后再合并**，不要直接推 `main`。

**与 `FB2_TASK.md` 的关系**：两者冲突时以本文件为准，涉及 `FB2_TASK.md` 第 7.1 节（对比方法）、第 11 节（执行顺序）和第 14 节（文献）。其余各节继续有效。

**启动方式**：Codex 不会自动派生子代理，必须明确要求。本机 codex-cli 0.153.2 的 `multi_agent` 功能为稳定版且默认开启，无需额外配置。把下面这段话交给 Codex：

```
读仓库根目录的 TASK_S3_SCOPING.md，按第 2 节的编排使用 .codex/agents/ 下的子代理执行。
第一波并行派出 library_paper_reviewer、literature_searcher、dk1_data_profiler；
三者交回后由你在主线程完成论文收录、文档写入、审计与提交，再写第 3 步的要求；
然后并行派出两个 structure_scout（分别负责依赖结构与基础模型、共形与风险校准）；
最后派出 redline_reviewer 审查全部改动，你修正后开 PR 并停下等我确认。
```

---

## 0. 已确定的研究定位（不要重新论证）

**要解决的问题**：前一天 07:30 要决定第二天承诺多少 aFRR 上调备用，这时第二天的**日前电价、aFRR 上调容量价、aFRR 上调激活量**都未知。新方法给出三者的**联合概率分布**，交给随机或鲁棒联合优化决定备用承诺与电量预留。论文比较「联合概率预测＋联合优化」对「独立点预测＋顺序决策」，看收益、备用未交付率、尾部风险三者的权衡；只改善预测误差、不改善决策结果，主张不成立。

**为什么必须是联合与概率（已有实测）**

| 实测结果 | 含义 |
|---|---|
| 价格点预测能争取的空间只占缺口 9.3%，LEAR 已拿到其中 77.2% | 继续提高点预测精度收益很小 |
| 事先知道激活量值 223,440 EUR，占缺口 57.1%，目前没有任何方法预测激活 | 必须把激活量纳入联合分布 |
| 点预测接联合优化后，LightGBM 这一组备用未交付率从 8.8% 升到 12.6% | 分布尾部要针对交付风险校准 |
| LightGBM 精度全面高于 LEAR，阈值规则下收益反而更低；换成联合优化后排名翻转 | 评价要看决策结果 |

**方法方向**（第 4 步后由用户确认）：按闸门顺序分解联合分布（先容量价与激活量，再给定容量结果的日前价）；针对「按分布决策后交付不了」的概率做校准，争取有限样本保证。

**三类方法的角色——必须分清**

| 角色 | 来源 | 论文里的位置 |
|---|---|---|
| 对比方法 | **解决同样问题的论文** | 主结果表 |
| 借用的结构 | 顶会方法 | 只作新方法的零件，在消融里逐个拿掉；**不作为对比对象** |
| 自建对照 | 独立点预测、独立概率预测、顺序决策等 | 主结果表，说明增益来源 |

**数据**：主实验只用 DK1。芬兰数据上的外部检验不在本任务范围。

## 1. 名称与仓库代号对照

首次出现写「名称（代号）」。

| 名称 | 代号 | 名称 | 代号 |
|---|---|---|---|
| 独立点预测 | FB0 | 只做日前能量 | B0 |
| 独立概率预测（消融） | — | 顺序决策 | B1 |
| 联合概率预测（本文） | FB2 | 点预测＋联合优化 | B2 |
| 联合概率预测＋第二天风光预测 | FB2+ | 随机或鲁棒联合优化（本文） | B3 |
| 完美预知上界 | Oracle | 完美价格参照 | oracle_price_reserve |
| LEAR / LightGBM | F01 / F08 | | |

---

## 2. 子代理编排

### 2.1 拆分原则

- **交给子代理**：读得多、输出冗长的工作——库内论文梳理、库外检索、数据分析、结构侦察、红线审查。子代理在独立上下文里工作，只交回摘要，避免主线程被中间输出淹没。
- **留在主线程**：强顺序、会写共享文件的工作——论文收录（采集器、catalog、manifest、对应表是共享索引，不能并行写）、研究文档写入、资源审计、提交、开 PR。
- **子代理数量够用即可**，不为拆而拆。

### 2.2 角色

定义文件在 `.codex/agents/`，每个文件的 `developer_instructions` 写明了规则与交回格式。

| 子代理 | 权限 | 负责 | 允许写入 |
|---|---|---|---|
| `library_paper_reviewer` | 只读 | 第 1 步：梳理库内 10 篇候选 | 无 |
| `literature_searcher` | 只读 | 第 1 步：库外检索与检索记录 | 无 |
| `dk1_data_profiler` | 可写 | 第 2 步：数据特点分析 | 仅第 4.5 节列出的三个路径 |
| `structure_scout` × 2 | 只读 | 第 4 步：分方向侦察可借用结构 | 无 |
| `redline_reviewer` | 只读 | 开 PR 前审查全部改动 | 无 |
| **主线程** | 可写 | 收录、写文档、审计、提交、开 PR；整合全部摘要 | 除 `dk1_data_profiler` 专属路径外的所有文件 |

### 2.3 执行顺序

| 波次 | 谁 | 做什么 | 前提 |
|---|---|---|---|
| 1（并行） | `library_paper_reviewer`、`literature_searcher`、`dk1_data_profiler` | 第 1 步两部分与第 2 步 | 无 |
| 主线程 A | 主线程 | 按第 3.5 节收录新论文、写 `PAPER_PLAN.md` 新节；按第 4.5 节写 `DATA_DICTIONARY.md` 新节；跑资源审计；第 1、2 步各提交一次 | 波次 1 全部交回 |
| 主线程 B | 主线程 | 第 3 步：写「问题 → 要求」 | 主线程 A 完成 |
| 2（并行） | `structure_scout`（依赖结构与基础模型）、`structure_scout`（共形与风险校准） | 第 4 步候选 | 主线程 B 完成 |
| 主线程 C | 主线程 | 收录第 4 步要引用的论文、写候选表、审计、提交第 3＋4 步 | 波次 2 全部交回 |
| 3 | `redline_reviewer` | 审查分支相对 `main` 的全部改动 | 主线程 C 完成 |
| 主线程 D | 主线程 | 修正所有「阻塞」「需修正」条目，开 PR，停下 | 审查交回 |

### 2.4 主线程整合规则

- **只采用子代理交回的、带出处的内容**；子代理标为「未确定」「未核实」「未检索」的，原样保留这些标注，**主线程不得自行补全或改写成确定结论**。
- 两个子代理结论冲突时，主线程回到原文核对后再写，并在提交信息里说明。
- 子代理失败、超时或无法联网：该部分记为「未完成」写入对应文档，不得由主线程凭记忆补编。

---

## 3. 第 1 步：同问题论文

### 3.1 什么算「同问题」，以及库内候选

本文的问题是储能在日前电能量与备用两个顺序市场里先预测、再决策。

| 级别 | 条件 |
|---|---|
| 一级 | 同问题、同市场：储能（含风光配套储能）参与日前加 aFRR，研究 DK1 或丹麦 |
| 二级 | 同问题、其他市场：储能参与日前或日内加备用市场，含预测与决策 |
| 三级 | 同一子问题：为储能或备用决策做预测，并用决策结果评价预测 |

库内候选（交 `library_paper_reviewer`）：丹麦风电混合储能日前加 aFRR（M08，仅来源卡）、西班牙预测—优化一体化框架（X02）、德国顺序市场优化（M07）、日前加 aFRR 储能经济评价（M06）、日内加 FCR 联合报价（X04）、储能参与市场的鲁棒时间离散（X05）、丹麦储能现场三年案例（X01）、概率预测与电池经济价值（F07）、决策导向的储能套利预测（L03）、共形预测用于日前与平衡市场（X03）。

### 3.2 每篇要判断的内容

级别；预测方式（点、独立概率、联合概率）及预测对象；决策方式；数据年份是否早于 2024 年 10 月；在 DK1 上按同一协议重新实现是否可行。

### 3.3 库外检索（交 `literature_searcher`）

- **数据库**：Scopus、Web of Science、IEEE Xplore、Google Scholar、DTU Orbit、arXiv 等能访问的都用；访问不了的写「未检索」。
- **关键词**：市场词（DK1、Denmark、Danish、Energinet、Nord Pool、Nordic、aFRR、FRR、regulating power、balancing、imbalance price、reserve capacity）× 主体词（battery、energy storage、BESS、hybrid power plant）× 方法词（probabilistic、quantile、scenario、joint、multivariate、copula、stochastic、robust、bidding、day-ahead）。
- **时间**：不限起点，逐篇标注数据年份。**DK1 的 aFRR 市场在 2024 年 10 月改为本地每日拍卖、按小时出清的容量市场**（`DATA_DICTIONARY.md` 第 24 行），此前论文的方法可借用，结果不能直接比。

### 3.4 入选对比方法的五个条件

1. 属于一级或二级；三级只在其方法能直接接入本文决策流程时入选；
2. 方法描述足够在 DK1 上重新实现，或有公开代码；
3. 能放进 07:30 信息集；
4. 决策部分能接入现有结算层，或预测部分能接入现有联合优化；
5. 发表载体已核验。

### 3.5 产出（主线程写）

- 要引用的新论文按已有流程入库：采集器（开放获取放 PDF，否则来源卡）、catalog、论文—数据对应表、分组索引、结果表，研究截止日同步更新。
- `research-foundation-2026/PAPER_PLAN.md` 新增一节「同问题对比方法」：候选表（名称、库编号、级别、预测方式、决策方式、数据年份、可否重新实现及理由、是否入选）与检索记录表（数据库、检索式、日期、命中数、初筛数、最终数，含未检索项）。
- 若没有任何一级或二级论文做联合概率预测，在该节写明，保留完整检索记录作为依据。

---

## 4. 第 2 步：DK1 数据特点（交 `dk1_data_profiler`）

### 4.1 范围——红线

**只分析回看期** 2025-10-01 至 2026-01-27，116 个可用交割日（剔除 2025-11-22、11-23、11-24）。**测试期（2026-01-28 起）数据不得以任何形式读入**，代码必须断言。分析结果将用来设计方法，用测试期数据等于拿测试集设计方法。

### 4.2 分析对象

| 目标 | 表与字段 | 粒度 |
|---|---|---|
| DK1 日前电价 | `day_ahead_prices.csv` 的 `DayAheadPriceEUR`（PriceArea=DK1） | 15 分钟 |
| aFRR 上调容量价 | `afrr_capacity_market.csv` 的 `UpPriceEUR` | 1 小时 |
| aFRR 上调激活量 | `imbalance_and_activation.csv` 的 `aFRRUpMW`（每 15 分钟 MWh） | 15 分钟 |
| aFRR 上调激活价 | `imbalance_and_activation.csv` 的 `aFRRVWAUpEUR` | 15 分钟 |

### 4.3 六个问题

| 编号 | 问题 |
|---|---|
| D1 | 各目标分布：均值、分位数、偏度、尖峰占比、负值占比、零值占比（激活量） |
| D2 | 三个目标之间的依赖：同时段与跨时段秩相关，上尾与下尾依赖 |
| D3 | 时间结构：日内各时段自相关，滞后 1 天与 7 天，工作日与周末差异 |
| D4 | 07:30 的信息量：只用当时可见的简单量（前一天同时段、前 7 天均值等）能解释第二天多少变化 |
| D5 | 数据量：84 天训练窗口内各目标有效样本数、极端值次数、联合分布需估计的维度 |
| D6 | 稳定性：回看期前后半段各项统计是否明显漂移 |

### 4.4 数据规则

UTC 主键，按民用交割日分组；缺失值保持缺失并写明有效样本数；`aFRRUpMW` 聚合到小时求和、不乘 0.25；所有输入通过 `available_at <= decision_time` 断言。**2026-03-04 的定义变更只涉及 mFRR 的 `SatisfiedDemand` 与 `DominatingDirection`**，不影响上述目标，回看期也早于该日。

### 4.5 文件

| 写入者 | 文件 |
|---|---|
| `dk1_data_profiler` | `experiments/s3_dk1_target_profile.py`；`configs/s3_dk1_target_profile.yaml`；`p1_paper/results/s3_dk1_target_profile/`（`summary.json` 含环境指纹与随机种子，D1–D6 各一张 csv） |
| 主线程 | `data/multi_market_energy_reserve/DATA_DICTIONARY.md` 新增「预测目标的数据特点（回看期）」一节，不超过 40 行，数字从结果文件读出，不手算 |

不建 `src/` 包：纯描述性统计，拆包属于 `AGENTS.md` 第 3 条禁止的预留抽象层。

## 5. 第 3 步：问题 → 要求（主线程）

- 在 `research-foundation-2026/RESEARCH_FRAMEWORK.md` 新增一节「联合概率预测必须满足的要求」，表列：编号、问题、来源、对方法的要求、怎么检验。
- 问题来源只有两类：第 2 步的数据特点（写明 D1–D6 哪一项及数值），第 1 步中同问题论文没解决的地方（写明哪篇、哪里）。不得写无依据的问题。
- 每条要求必须可检验，例如「极端激活时段的覆盖率不低于名义水平」。
- 该节不超过 50 行，全文件不超过 400 行。

## 6. 第 4 步：可借用结构（交两个 `structure_scout`，只列不实现）

- 方向一：依赖结构与基础模型，检索起点如低秩高斯 Copula 过程、TACTiS / TACTiS-2、Moirai、Chronos。
- 方向二：共形与风险校准，检索起点如共形风险控制、多步时序的 Copula 共形预测、共形 PID 控制；扩散生成模型（TimeGrad、CSDI）只评估小样本风险。
- **以上仅为检索起点，逐篇核验载体后才能写入。**
- 主线程把两份交回合并成一张候选表，写进本文件第 5 节在 `RESEARCH_FRAMEWORK.md` 新增的同一节里：结构、代表论文与已核验载体、满足哪条要求、84 天窗口下的小样本风险、官方代码。要引用的论文按第 3.5 节入库。
- **只列候选，不选定、不实现。**

## 7. 对比方案草案（用户确认后另开任务实现）

| 侧 | 类别 | 方法 |
|---|---|---|
| 预测 | 已有点预测基线 | 持续法、LEAR、LightGBM（保留） |
| 预测 | 自建对照 | 独立点预测；独立概率预测（边际与本文相同，去掉相关性） |
| 预测 | 简单联合基线 | 边际分布＋训练期历史误差组合 |
| 预测 | **同问题论文的方法** | 第 1 步入选者，按 DK1 协议重新实现 |
| 预测 | 本文 | 按闸门顺序分解、针对交付风险校准的联合概率预测 |
| 决策 | 对照 | 独立点预测＋顺序决策；各点预测＋联合优化 |
| 决策 | 已有工作 | 同问题论文的预测与决策方法 |
| 决策 | 本文与消融 | 本文预测＋随机或鲁棒联合优化；独立概率预测＋同一优化 |
| 决策 | 参照 | 完美价格参照、完美预知上界，写明各自信息集 |

---

## 8. 不要做

1. 不实现任何预测模型或优化方法。
2. **第 2 步不得读取 2026-01-28 起的任何数据。**
3. 不把顶会方法列为对比方法。
4. 不改冻结协议：时间划分、窗口、指标定义、缺失值规则、储能参数、结算公式。
5. 不改 `src/epf_harness/` 与已有实验文件，不重跑已有实验。
6. 不补缺失值；不把芬兰、德国、RTS-GMLC 数据与 DK1 行级拼接。
7. 未核验载体的论文不写入研究文档；「未检索」不得写成已检索。
8. 不往 `EXECUTION_PLAN.md` 追加（已 397 行）。
9. 子代理不得写出自己允许范围之外的文件；两个子代理不得写同一个文件。
10. 不新建 README、SUMMARY、CHANGELOG、`*_GUIDE.md`、`*_NOTES.md`、`*_REPORT.md`；不用 `git add -A` 或 `git add .`；不直接推 `main`。

## 9. 验收

**第 1 步**：`PAPER_PLAN.md` 两张表完整；新入库论文通过资源审计：

```bash
cd /public/ZLCODE/electricity-price-forecasting-research && /public/ZLCODE/.venvs/r1_lear_de/bin/python scripts/audit_multi_market_resources.py
```

退出码为 0。若在 git 工作树里只缺被 `.gitignore` 排除的 `day_ahead_prices_raw.json`，从主检出硬链接补齐后重跑，不得放宽断言。

**第 2 步**：

```bash
cd /public/ZLCODE/electricity-price-forecasting-research && OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 MKL_NUM_THREADS=1 /public/ZLCODE/.venvs/r1_lear_de/bin/python experiments/s3_dk1_target_profile.py --config configs/s3_dk1_target_profile.yaml
```

「全部交割日早于 2026-01-28」断言通过且为 116 天；`summary.json` 含环境指纹与种子，D1–D6 齐全并有样本数；重跑一致；`git diff --stat p1_paper/results/` 只出现 `s3_dk1_target_profile/`。

**第 3、4 步**：`RESEARCH_FRAMEWORK.md` 新节含两张表，每条问题有依据、每条要求可检验，全文件不超过 400 行。

**审查**：`redline_reviewer` 交回表中不再有「阻塞」或「需修正」条目。

## 10. 提交与停止点

- 第 1 步、第 2 步、第 3＋4 步各提交一次；审查后的修正单独提交。commit message 写明新增内容、关键发现，以及哪部分由哪个子代理完成。
- 开 PR（`codex/s3-scoping` → `main`），描述里用三五句总结：找到几篇同问题论文、有没有做联合概率预测的、数据最突出的三个特点、最关键的三条要求。
- **开 PR 后停下，等用户确认方法设计与对比方案。**
