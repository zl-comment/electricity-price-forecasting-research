# AGENTS.md

本仓库是科研仓库，不是产品工程。以下规则对所有 AI 代理生效，优先级高于任何"看起来更周全"的默认行为。

规则写成禁令是刻意的：可判定，可检查。

## 1. 产出边界

- 禁止新建文件，除非任务里明确写了要新建。默认改现有文件。
- 禁止新建 `README.md`。全仓库只允许三个：根目录、`data/`、`paper/`。
- 禁止新建 `SUMMARY.md`、`CHANGELOG.md`、`*_GUIDE.md`、`*_NOTES.md`、`*_REPORT.md` 及任何用于"说明本次改动"的文件。改动说明写在 commit message 里。
- 禁止文件名带日期戳。历史版本由 git 承担，不由文件名承担。
- `research-foundation-2026/` 下只允许存在三个文件：`RESEARCH_FRAMEWORK.md`（研究什么）、`EXECUTION_PLAN.md`（怎么做）、`PAPER_PLAN.md`（写成什么）。方向变更时修改它们，不新增。数据事实写进 `data/multi_market_energy_reserve/DATA_DICTIONARY.md`，不写进这三份。
- `data/` 与 `paper/` 下的采集产物（PDF、CSV、JSON、压缩包）进版本控制，每个必须在对应 `download_manifest.json` 里有 SHA-256 条目，能由 `scripts/collect_multi_market_resources.py` 重新获取。脚本生成的索引文件（`catalog.json`、`download_manifest.json`、`data_audit.json`、`paper_data_alignment.json`）例外。既无 manifest 条目、又不是索引文件的二进制文件不得提交。
- `p1_paper/` 下的实验产物进版本控制。每个实验目录必须有一个汇总文件记录环境指纹和随机种子，缺这两项的结果不提交。
- 单个文件超过 20 MB 时停下报告，不要直接提交。API 原始响应（`*_raw.json`）与其无损转写的 CSV 是同一份数据的两种编码，超过 20 MB 时只提交 CSV，原始响应由 `.gitignore` 排除，manifest 保留其 URL 与 SHA-256。
- 新克隆的仓库缺少上述被排除的文件，`scripts/audit_multi_market_resources.py` 会因校验和缺失而失败。先运行采集器补齐，不要为通过审计而放宽断言。
- 本地运行产物（`.claude/`、`__pycache__/`）由 `.gitignore` 拦截，不进版本控制。禁止用 `git add -A` 或 `git add .`，按路径逐项暂存。

## 2. 文档写法

- 术语边界、免责规则、数据角色定义、质量分层标准只在 `CONVENTIONS.md` 里定义一次。其他文档引用链接，禁止重述。
- 禁止引言段、结语段、"综上所述"、"希望对你有帮助"、"本文档旨在"。第一句直接给结论。
- 禁止在文档里重复相邻文档已经写过的内容。需要交叉引用时给链接。
- 禁止用形容词描述工作量或质量（"全面""详尽""高质量""大幅"）。给数字或不给。
- 表格优先于并列段落。三项以上的并列关系一律用表格。
- 单个 md 文件超过 400 行时停下来报告，不要继续追加。

## 3. 代码写法

- 禁止 `try/except` 包裹，除非任务明确要求处理某个具体异常。失败要崩，不要吞。
- 禁止 fallback 分支、默认值兜底、向后兼容层，除非明确要求。
- 禁止 mock 数据、示例数据、占位数据。数据缺失就报错停下并说明缺什么。
- 禁止为"将来可能需要"而写的配置项、抽象层和参数开关。
- 禁止在未被要求时重构、重命名、调整目录结构或统一代码风格。
- 一个函数只做一件事。超过 60 行拆开。
- 超参只允许出现在一个 yaml 配置文件里，禁止散落在代码中。
- 随机种子必须显式传入并写入结果文件。

## 4. 实验红线

以下改动即使看起来是改进，也禁止在未被明确要求时进行：

- 修改滚动窗口长度、重校准周期、训练/验证/测试的时间划分。
- 修改评价指标的定义或计算口径。
- 修改缺失值处理规则。当前规则是保持缺失，不补零、不插值、不外推。
- 对时间序列做随机切分。任何划分必须按时间。
- 让某个方法获得其他方法看不到的信息，或使用更宽松的可行域约束。
- 升级或更换 Python、numpy、pandas、scipy、scikit-learn、statsmodels 及任何科学计算依赖。

评估协议被静默修改会使此前所有实验结果作废且难以察觉。任何涉及以上六项的改动，先停下来说明理由并等确认。

依赖版本是结果的一部分，不是环境细节。R1 已实测：`LassoLarsIC` 在 scikit-learn 1.0 改了 AIC 判据并删除 `normalize` 参数，同一份 LEAR 代码在新版下无法复现作者结果，`n < p` 时直接抛异常。R1 的复现结论只在 `requirements-r1.txt` 固定的版本下成立。每个结果文件必须记录环境指纹（python、scikit-learn、numpy、pandas、scipy、statsmodels 的精确版本），缺指纹的结果不作数。

## 5. 数据纪律

- 观测量与模型生成量必须分栏记录，禁止混写。DK1 的日前价格、aFRR 容量价格、不平衡与激活量是原始市场标签；储能报价、成交、SOC 和控制动作由研究模型产生。
- RTS-GMLC 优化器产生的价格、成交量、弃电和缺口一律标注为模型出清结果，禁止表述为实网观测。
- 论文作者报告的数字一律标 `paper-reported`，禁止写成本仓库已复现。
- 四套数据（DK1 / 芬兰 aFRR / RTS-GMLC / EPF-DE）角色不同，禁止行级拼接成同一个系统。
- 禁止把决策时刻尚不可见的信息作为输入。每个特征都要有 `available_at <= decision_time` 的检查。

## 6. 工作方式

- 预计改动超过 30 行时，先给改动计划（涉及哪些文件、各改什么）并停下等确认，不要直接动手。
- 一次只做任务里写明的事。发现顺带的问题，报告出来，不要顺手改。
- 改完只报三件事：改了哪些文件、每个文件改了什么、怎么验证。不要复述代码逻辑，不要总结意义。
- 不确定时问，不要猜着补全。

## 7. 每次任务的输入格式

提任务的人应按此格式给出。缺失项按"禁止"处理。

```
目标：一句话
改哪里：文件与函数名
不要做：本次额外的禁止项
验收：跑什么命令、看到什么算通过
```
